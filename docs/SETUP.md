# Setup

Setup and deployment now live in the authoritative system spec:

➡️ **[`SPEC.md` §12 — Setup and deployment](./SPEC.md#12-setup-and-deployment)**

That section covers prerequisites, web-app-only vs. full-pipeline setup, the
`config.yaml` / `.env` / resume inputs, Ollama and Telegram setup, the one-off test
pass, and the shared-volume layout — kept in one place so it can't drift from the
rest of the spec.

Quick start is in the [root README](../README.md#quick-start).
